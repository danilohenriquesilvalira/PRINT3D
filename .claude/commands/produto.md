---
description: Adiciona ou edita um produto no projeto PRINT3D. Uso: /produto [descrição do produto]
---

Tarefa: adicionar ou editar um produto no projeto PRINT3D.

Contexto:
- Produtos hardcoded em `src/app/page.tsx` (arrays: `products` para featured, etc.)
- Interface em `src/types/product.types.ts` — leia antes de criar
- Imagens ficam em `public/images/products/`
- O componente que renderiza é `src/components/common/ProductCard.tsx`

Instruções:
1. Leia `src/types/product.types.ts` para ver os campos obrigatórios
2. Leia `src/app/page.tsx` para ver o padrão dos produtos existentes
3. Adicione o produto seguindo exatamente o mesmo padrão
4. Se precisar de imagem nova, indique o caminho sugerido em `public/images/products/`
5. Textos em português

Detalhes do produto a adicionar/editar: $ARGUMENTS
